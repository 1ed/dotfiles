#
# This is a shell fragment that initializes rbenv, if it
# has not been inited yet.
#
if ! echo $PATH | grep -q rbenv; then
  export PATH="/home/egabor/.rbenv/bin:$PATH"
  eval "$(rbenv init -)"
fi
