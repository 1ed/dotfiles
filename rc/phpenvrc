#
# This is a shell fragment that initializes phpenv, if it
# has not been inited yet.
#
if ! echo $PATH | grep -q phpenv; then
  export PATH="/home/egabor/.phpenv/bin:$PATH"
  eval "$(phpenv init -)"
fi
